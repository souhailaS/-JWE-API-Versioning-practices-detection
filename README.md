# WEB API VERSIONING PRACTICES 
In this repository, we share the list of distinct version identifiers we detected in APIs metadata and th list of regex based on which the identifiers parser is built. 